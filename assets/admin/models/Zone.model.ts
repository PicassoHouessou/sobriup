export interface Zone {
    id: string;
    name: string;
    description: string;
    createdAt: string;
    createdAtAgo: string;
}

export interface ZoneEdit {
    id: string;
    name: string;
    description: string;
}
